# Реестр тестов — FFmpeg Visual

Что покрыто и что нужно покрыть. Правило №7 CLAUDE.md: заметил недостающий тест —
допиши строку в «🔴 Нужно покрыть», не анализируя проект заново.

**Запуск:** `npm test` (Vitest, `vitest run`). Тесты лежат рядом с кодом (`*.test.ts`).

## ✅ Что уже покрыто

| Что | Где | Тестов |
|---|---|---|
| Каталог фильтров: индекс, уникальность id, поиск, группировка | `src/shared/lib/ffmpeg/catalog/catalog.test.ts` | 5 |
| `toFilterString` каждого фильтра (граф значений → строка) | там же (table-driven) | 4 |

**Итого: 9 ✅**

## 🔴 Нужно покрыть (по мере появления кода)

- Генератор команды `generate.ts` (граф → массив аргументов) — когда появится.
- Валидатор `validate.ts` (циклы, висячие ноды, незаполненные параметры) — когда появится.
- Хук `useGraph` (добавление нод, соединение) — компонентные тесты (нужен @testing-library).
- Tauri-команды (`run_ffmpeg`, `probe_media`) — отдельно/вручную (тонкий слой).
