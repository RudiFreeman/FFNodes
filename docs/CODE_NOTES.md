# CODE_NOTES — журнал наблюдений

Техдолг и странности, замеченные по ходу работы, но не относящиеся к текущей задаче.
Правило №6 CLAUDE.md: не чинить на месте — записать сюда. Разбираешь долг — бери `🔴 open`.

| # | Статус | Что | Где | Заметка |
|---|---|---|---|---|
| N-001 | 🔴 open | Контролы React Flow (zoom/fit кнопки) не стилизованы под тёмную тему — белый артефакт в углу холста | `src/widgets/NodeCanvas/NodeCanvas.tsx` | `<Controls>` рендерит светлые иконки. Нужно переопределить CSS-переменные React Flow (`--xy-controls-button-*`) под палитру UI.md. Косметика, не блокер |
| N-002 | ✅ closed | Заголовок вкладки браузера/окна — дефолтный «Tauri + React + Typescript» | `index.html` | Исправлено в feat/catalog-nodes: `<title>` → «FFmpeg Visual» |
| N-003 | 🔴 open | Удаление фильтра из середины цепочки рвёт её (связи удаляются вместе с нодой) | `src/features/add-node/useGraph.ts` | Генератор честно покажет «соедини ноды», но UX плохой. Нужна авто-перецепка: при удалении ноды соединить её предшественника с преемником. Продумать в итерации про редактирование графа |
| N-004 | 🔴 open | `probe_media`/`run_ffmpeg`: путь, начинающийся с `-`, теоретически трактуется ffmpeg/ffprobe как флаг (argument injection) | `src-tauri/src/ffmpeg.rs` | Низкий риск (пути из системных диалогов начинаются с `/`). Ужесточение: проверять, что путь не начинается с `-`, либо префиксовать `./`. Security-review (file-probe, render): серьёзных дыр нет, это профилактика |
| N-005 | 🔴 open | Рендер нельзя отменить — `run_ffmpeg` идёт до конца, нет кнопки «Стоп» | `src-tauri/src/ffmpeg.rs`, `src/features/run-render/useRender.ts` | UX-недостаток (не security). Длинный рендер не прервать. Нужно: хранить child process, команда `cancel_render` (kill), кнопка «Отмена» во время рендера |
| N-006 | 🔴 open | Дефолт «Изменить размер» = ширина 1280 раздувает вертикальное видео (1080×1920 → 1280×2276) | `src/shared/lib/ffmpeg/catalog/resize.ts` | Дефолт 1280 подразумевает горизонтальное видео. Для вертикального это увеличение, не уменьшение — неинтуитивно. Нужны: пресеты (720p/1080p/половина), подсказка «текущий размер», либо «по длинной стороне». Обсудить UX с Aleksei |
| N-007 | 🔴 open | Несочетаемые операции в цепочке не валидируются (напр. «Извлечь аудио» -vn + видеофильтр; «Убрать звук» -an + «Извлечь аудио» -vn) | `src/shared/lib/ffmpeg/generate.ts`, validate.ts (будущий) | Команда собирается, не падает, но бессмысленна. Нужна валидация осмысленных комбинаций: предупреждать или блокировать конфликтующие операции. Добавить в будущий validate.ts |
| N-008 | 🔴 open | GIF без двухпроходной палитры — качество цветов хуже (бандинг) | `src/shared/lib/ffmpeg/catalog/gif.ts` | Сейчас однопроходный `fps,scale` + `-f gif` (в рамках линейной vf-модели). Качественный GIF: `split[a][b];[a]palettegen[p];[b][p]paletteuse` — требует filter_complex. Сделать, когда появится поддержка multi-input графов |
| N-009 | 🔴 open | «Изменить скорость» меняет только видео (setpts), звук рассинхронизируется | `src/shared/lib/ffmpeg/catalog/speed.ts` | Для синхронной скорости нужен и `atempo` на аудио (отдельный фильтр аудиопотока). Сейчас vf-модель не трогает аудио-цепочку. Добавить при поддержке аудиофильтров |
| N-010 | 🔴 open | Размер файла результата (`size_bytes`) не предсказывается — зависит от битрейта/CRF/контента | `src/shared/lib/ffmpeg/predict.ts` | Предсказание «После» оставляет размер от входа, в UI помечен «≈ оценка». Точно его до рендера не посчитать. Возможная грубая оценка по битрейту/CRF — отдельная задача; пока честнее показывать «≈». После рендера панель показывает реальный размер (ffprobe) |
| N-011 | ✅ closed | Превью-кадры копятся во временной папке — `extract_frame` пишет новый JPG на каждый вызов, не удаляя старые | `src-tauri/src/ffmpeg.rs` | Закрыто в fix/temp-frame-cleanup. Чистая `frames_to_cleanup(existing, keep)` (всё кроме N последних; имена по наносекундам = сортировка хронологична) + хелпер `cleanup_old_frames(keep)` сканит temp по маске `ffmpeg-visual-frame-*.jpg`. Вызов: в конце `extract_frame` (keep=4 — До+После+запас на дебаунс), при старте в `lib.rs setup` (keep=0 — чистим прошлые сессии). 4 теста |
| N-012 | 🔴 open | Кадр «После» для временны́х фильтров (trim/speed/setpts) может быть нерепрезентативен | `src/shared/lib/ffmpeg/frame.ts`, `src-tauri/src/ffmpeg.rs` | Кадр берём в фиксированный момент (середина исходника) и прогоняем через vf. trim/setpts меняют временну́ю ось — в этот момент кадра может не быть (чёрный/пустой) или он смещён. Для пространственных фильтров (scale/crop/flip/color) всё корректно. Продумать выбор момента с учётом trim |
| N-013 | ✅ closed | Клик по ноде ронял весь UI («Maximum update depth exceeded», чёрный экран) | `src/features/add-node/useGraph.ts` | Эффект синхронизации info/predictedOutput в data нод вызывал `setNodes` без проверки на изменение: setNodes → новый `nodes` → useMemo `graph` → `predictOutput` отдаёт новый объект → эффект снова → цикл. Латентный баг, вскрылся при возврате `graph` из хука (feat/preview-frame). Фикс: сравнение по содержимому, возвращаем `prev` если info и предсказание не изменились. Добавлен `ErrorBoundary` (раньше краш был немым чёрным экраном) |
